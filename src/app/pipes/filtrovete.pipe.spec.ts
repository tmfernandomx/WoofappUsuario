import { FiltrovetePipe } from './filtrovete.pipe';

describe('FiltrovetePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrovetePipe();
    expect(pipe).toBeTruthy();
  });
});
